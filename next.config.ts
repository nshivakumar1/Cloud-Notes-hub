import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: standalone output is used for Docker, but Static Web Apps works better with default output
  // output: 'standalone',
};

export default nextConfig;
